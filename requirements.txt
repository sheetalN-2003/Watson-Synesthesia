# Core framework
streamlit

# Image processing
opencv-python-headless
Pillow

# Hugging Face Transformers (for BLIP, text generation etc.)
transformers

# PyTorch (CPU-only, compatible with Streamlit Cloud)
torch
torchvision
torchaudio

# Text-to-Speech
TTS

# Object Detection (YOLOv8)
ultralytics
